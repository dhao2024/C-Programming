#include <stdio.h>

int main(void)
{
    int a, b, c, d, e, f, g, h, i, j;
    float k, l, m, n, o, p, q;

    printf("%d %d %d %d %d %d %d %d %d %d\n", a, b, c, d, e, f, g, h, i, j);
    printf("%f %f %f %f %f %f %f\n", k, l, m, n, o, p, q);

    return 0;
}